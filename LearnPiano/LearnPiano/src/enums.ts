export enum KeyColors {
    WHITE,
    BLACK,
}

export enum KeyZIndexes {
    WHITE = 0,
    BLACK = 1,
}

export enum SpriteIndex {
    NOT_SELECTED = 4,
    SELECTED = 5,
}

export enum SelectOptions {

    NOT_SELECTED = 1,
    SELECTED = 2,
}

export enum GameModes {

    PIANO_SHUFFLER = 1,
}